import {testTypes} from "./test/testTypes";
import {authTypes} from "./auth/authTypes";
import {windowTypes} from "./window/windowTypes"
import { headerTypes } from "./header/headerTypes";
import { accessTypes } from './access/accessTypes';

export const types = {
    testTypes,
    authTypes,
    windowTypes,
    headerTypes,
    accessTypes
}